framework:
    cache:
        prefix_seed: experiment
        app: cache.adapter.redis
        system: cache.adapter.redis
        default_redis_provider: 'redis://%env(REDIS_HOST)%:%env(REDIS_PORT)%'
